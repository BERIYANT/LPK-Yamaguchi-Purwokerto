<x-app-layout><x-slot name="pageTitle">Data Sensei</x-slot>
<div class="mb-7"><p class="eyebrow">Tenaga Pengajar</p><h2 class="display-title">Data Sensei</h2><p class="mt-2 text-sm text-black/50">Kelola profil dan akun tenaga pengajar.</p></div>
@if(session('success'))<div class="mb-5 rounded-xl bg-green-50 p-4 text-sm font-bold text-green-700">{{ session('success') }}</div>@endif
<div class="grid gap-5 xl:grid-cols-[1.35fr_.65fr]"><section class="panel overflow-hidden p-0"><div class="overflow-x-auto"><table class="data-table"><thead><tr><th>Nama</th><th>Kode</th><th>Bidang</th><th>Kontak</th><th>Status</th></tr></thead><tbody>@forelse($sensei as $item)<tr><td><b>{{ $item->name }}</b><small>{{ '@'.$item->username }}</small></td><td>{{ $item->senseiProfile?->sensei_code ?? '—' }}</td><td>{{ $item->senseiProfile?->teaching_field ?? '—' }}</td><td>{{ $item->senseiProfile?->phone ?: ($item->phone ?: '—') }}</td><td><span class="status-pill">{{ $item->senseiProfile?->status ?? 'Profil belum lengkap' }}</span></td></tr>@empty<tr><td colspan="5" class="empty-state">Belum ada data sensei.</td></tr>@endforelse</tbody></table></div></section>
<section class="panel"><div class="panel-head"><h3>Tambah Sensei</h3></div>@if($errors->any())<div class="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('admin.sensei.store') }}" class="space-y-4">@csrf<label class="block text-xs font-bold">Kode Sensei<input name="sensei_code" value="{{ old('sensei_code') }}" class="input mt-2 w-full" required></label><label class="block text-xs font-bold">Nama lengkap<input name="full_name" value="{{ old('full_name') }}" class="input mt-2 w-full" required></label><label class="block text-xs font-bold">Username<input name="username" value="{{ old('username') }}" class="input mt-2 w-full" required></label><label class="block text-xs font-bold">Nomor HP<input name="phone" value="{{ old('phone') }}" class="input mt-2 w-full"></label><label class="block text-xs font-bold">Bidang mengajar<input name="teaching_field" value="{{ old('teaching_field') }}" class="input mt-2 w-full"></label><label class="block text-xs font-bold">Alamat<input name="address" value="{{ old('address') }}" class="input mt-2 w-full"></label><label class="block text-xs font-bold">Status<select name="status" class="input mt-2 w-full"><option value="aktif">Aktif</option><option value="nonaktif">Nonaktif</option></select></label><button class="primary-button w-full">Simpan Sensei</button></form></section></div>
</x-app-layout>