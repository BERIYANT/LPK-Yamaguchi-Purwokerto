<x-app-layout><x-slot name="pageTitle">Penilaian</x-slot><div class="mb-7"><p class="eyebrow">Evaluasi Akademik</p><h2 class="display-title">Hasil pengumpulan tugas.</h2></div><div class="panel overflow-hidden p-0"><div class="overflow-x-auto"><table class="data-table"><thead><tr><th>Peserta</th><th>Tugas</th><th>Dikumpulkan</th><th>Nilai</th><th>Status</th></tr></thead><tbody>@forelse($submissions as $submission)<tr><td><b>{{ $submission->student?->name ?? 'Peserta tidak ditemukan' }}</b></td><td>{{ $submission->task?->title ?? '—' }}</td><td>{{ $submission->submitted_at ? \Illuminate\Support\Carbon::parse($submission->submitted_at)->translatedFormat('d M Y, H:i') : '—' }}</td><td><b>{{ $submission->score ?? $submission->grade ?? '—' }}</b></td><td><span class="status-pill">{{ is_null($submission->score) && is_null($submission->grade) ? 'Perlu dinilai' : 'Selesai' }}</span></td></tr>@empty<tr><td colspan="5" class="empty-state">Belum ada pengumpulan tugas.</td></tr>@endforelse</tbody></table></div><div class="border-t border-black/[.06] p-5">{{ $submissions->links() }}</div></div></x-app-layout>